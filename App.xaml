<Application x:Class="WinRemoteSharp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- ========== 颜色画刷 ========== -->
        <SolidColorBrush x:Key="MintMainBrush" Color="#FF20B2AA"/>
        <SolidColorBrush x:Key="MintDeepBrush" Color="#FF178F87"/>
        <SolidColorBrush x:Key="MintWhiteBrush" Color="#FFF5FFFE"/>
        <SolidColorBrush x:Key="SkyMainBrush" Color="#FF4A90D9"/>
        <SolidColorBrush x:Key="SkyDeepBrush" Color="#FF357ABD"/>
        <SolidColorBrush x:Key="TitleBarBrush" Color="#FF2C3E50"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#FFE74C3C"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#FF27AE60"/>

        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FF2C3E50"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FF7F8C8D"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#FFBDC3C7"/>
        <SolidColorBrush x:Key="TextLightBrush" Color="#FFFFFFFF"/>

        <SolidColorBrush x:Key="BgLightBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="BgCardBrush" Color="#FFF8FAFB"/>

        <SolidColorBrush x:Key="CardBorderBrush" Color="#FFD5DCE6"/>
        <SolidColorBrush x:Key="BorderLightBrush" Color="#FFE8ECF0"/>

        <!-- ========== 动画 Storyboard ========== -->
        <Storyboard x:Key="StatusDotPulse" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.3" Duration="0:0:0.8"/>
        </Storyboard>

        <!-- ========== Window 样式 ========== -->
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Microsoft YaHei, SimSun, sans-serif"/>
            <Setter Property="Background" Value="{StaticResource MintWhiteBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <!-- ========== TextBox 样式 ========== -->
        <Style TargetType="TextBox">
            <Setter Property="CaretBrush" Value="{StaticResource MintMainBrush}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource MintMainBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{StaticResource BgLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource MintMainBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource MintDeepBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ========== PasswordBox 样式 ========== -->
        <Style TargetType="PasswordBox">
            <Setter Property="CaretBrush" Value="{StaticResource MintDeepBrush}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource MintMainBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{StaticResource BgLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource MintMainBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource MintDeepBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ========== Button 样式 ========== -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource MintMainBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource MintDeepBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== DangerButton ========== -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC0392B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ========== SecondaryButton ========== -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource TextSecondaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF636E72"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ========== GroupBox 样式 ========== -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="14"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== TabControl / TabItem 样式 ========== -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Background="Transparent"
                                Padding="{TemplateBinding Padding}"
                                Cursor="Hand">
                            <ContentPresenter Content="{TemplateBinding Header}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource MintMainBrush}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource MintDeepBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== ComboBox 样式 ========== -->
        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="Background" Value="{StaticResource BgLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <!-- ========== CheckBox 样式 ========== -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- ========== ScrollViewer 样式 ========== -->
        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent"/>
        </Style>

    </Application.Resources>
</Application>
